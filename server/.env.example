# DebateAI proxy — SERVER-SIDE secrets. Never use the EXPO_PUBLIC_ prefix here,
# and never commit the real .env. These stay on the server only.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
ELEVENLABS_API_KEY=

# Optional: port to listen on (default 8787)
PORT=8787
